# Local — não é versionado (ver .env.example). Reinicie o Vite após alterar.
# Para apontar para a API em nuvem, troque a URL abaixo (sem barra no fim).
VITE_API_URL=https://batmotor-api-w3ad.onrender.com
VITE_USE_MOCK=false
